<%= form_for(@command_item) do |f| %>
  <% if @command_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@command_item.errors.count, "error") %> prohibited this command_item from being saved:</h2>

      <ul>
      <% @command_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :shot_id %><br />
    <%= f.number_field :shot_id %>
  </div>
  <div class="field">
    <%= f.label :command_id %><br />
    <%= f.number_field :command_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
