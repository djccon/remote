<%= form_for(@ball_flight) do |f| %>
  <% if @ball_flight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ball_flight.errors.count, "error") %> prohibited this ball_flight from being saved:</h2>

      <ul>
      <% @ball_flight.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_measurement_time %><br />
    <%= f.text_field :first_measurement_time %>
  </div>
  <div class="field">
    <%= f.label :last_measurement_time %><br />
    <%= f.text_field :last_measurement_time %>
  </div>
  <div class="field">
    <%= f.label :positions %><br />
    <%= f.text_area :positions %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
